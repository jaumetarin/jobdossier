export type TopCompany = {
  company: string;
  count: number;
};

export type TopTechnology = {
  technology: string;
  count: number;
};

export type SalaryByStack = {
  technology: string;
  averageSalary: number;
  sampleSize: number;
};
